#!/usr/bin/env bash
# consult-browser — durable owner handoff; never launches or controls Chrome.
set -euo pipefail
HERE="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
. "$HERE/../scripts/env.sh"
exec "$DANUS_PY" -I -B -m danus.strategy.browser_cli "$@"
